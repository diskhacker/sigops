# Session: Sprint 1.5 — README Honesty + Homelab Deployment
Date: 2026-04-17

## What was completed
- PR #8 — `docs(readme): rewrite to match actual shipped features` (merged to main)
- PR #9 — `ops: GHCR image build workflow + db:migrate script` (merged to main)

## What changed
- `README.md` rewritten. Removed unimplemented feature claims: FlowBuilder, bidirectional adapters for PagerDuty/Datadog/Grafana, WebSocket agent gateway, email signal polling, rate limiting on ingest, OpenTelemetry / Prometheus metrics, and Cloud claims without code (SSO, audit-log export to SIEM, advanced custom RBAC, multi-region). Added `## Roadmap` section for planned items. Added `## SigOps CLI` section. Scoped `## SigOps Cloud` to features that exist in code (AI Advisor, Knowledge Engine, On-Call, War Room, Postmortem, Status Page, SLA Tracker, Marketplace).
- `.github/workflows/docker.yml` added. Builds `ghcr.io/diskhacker/sigops-server` and `ghcr.io/diskhacker/sigops-ui` on `v*.*.*` tags and manual dispatch.
- `server/package.json` — added `db:migrate` script (`drizzle-kit migrate`). `db:push` stays for dev only.

## Decisions made
- README reflects shipped code, not aspirations. Aspirations go to `## Roadmap`.
- Server image build context is `./server` (not repo root) because the server Dockerfile lives under `server/`.
- Migrations run explicitly, not auto-on-boot — `db:migrate` is the path, operators call it from `cluster-deploy` after deploy.

## What's pending
- CLAUDE.md still documents `Agent WebSocket gateway` in the Module Build Order and `sigops-core` as a repo name. Not corrected in this sprint — scoped to a follow-up that can touch the spec sections safely.
- `db:generate` has not been run on main — no migration SQL is committed under `server/drizzle/` yet. Needs a developer local run + PR.
- No actual release tag has been cut, so no image has been published to GHCR yet.
- Rate limiting on signal ingestion (listed in Roadmap) — still unimplemented.

## Next steps
1. Developer runs `pnpm db:generate` locally and commits the generated migration SQL.
2. Tag `v0.1.0` to trigger the new docker.yml workflow and publish first images.
3. Update CLAUDE.md to replace `sigops-core` references and soften `WebSocket gateway` language to match code.
4. Begin FlowBuilder + WSS agent transport tracks in a future sprint.
