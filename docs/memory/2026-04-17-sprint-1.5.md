# Memory: Sprint 1.5

## Key facts for future sessions
- README claims now match actual code. No FlowBuilder, no bidirectional adapters, no WSS agent gateway, no metrics export. All listed under Roadmap instead.
- Docker images build via `.github/workflows/docker.yml` and publish to `ghcr.io/diskhacker/sigops-server` + `ghcr.io/diskhacker/sigops-ui`. Trigger: tag `v*.*.*` or manual dispatch.
- Homelab deployment lives in `cluster-deploy` (separate repo / tarball handoff). Compose pulls the GHCR images above.
- DB migrations use `pnpm db:migrate` (drizzle-kit migrate). NEVER use `db:push` in production.
- `JWT_SECRET` must be identical across UAP and SigOps (verified locally via HS256).
- Server Dockerfile already runs as `USER node`. No non-root fix needed in this repo.
- Agent transport is HTTP long-polling, not WebSocket. Do not add WSS references to code or docs without implementing the transport.

## Files modified (merged to main)
- `README.md`
- `.github/workflows/docker.yml` (new)
- `server/package.json`
- `docs/session/2026-04-17-sprint-1.5.md` (new)
- `docs/memory/2026-04-17-sprint-1.5.md` (new)
- `CLAUDE.md` (prepended recent-changes note)

## PRs merged
- https://github.com/diskhacker/sigops/pull/8 — docs(readme): rewrite to match actual shipped features
- https://github.com/diskhacker/sigops/pull/9 — ops: GHCR image build workflow + db:migrate script
